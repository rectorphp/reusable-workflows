{
    "require": {
        "php": ">=8.1",
        "rector/rector-src": "^0.14.8"
    },
    "require-dev": {
        "symfony/dependency-injection": "^6.1",
        "symplify/easy-ci": "^11.0"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "rector/extension-installer": true
        }
    }
}
